{
  "name": "lucent",
  "description": "Call-scoped Clear Signing presentation checks for EVM signing agents",
  "version": "0.2.1",
  "transport": "stdio",
  "command": ".venv/bin/python",
  "args": ["scripts/mcp_server.py"],
  "tools": ["preflight_transaction", "check_descriptor", "explain_signature", "scan_contract"],
  "notes": "Run from the repo root. Requires the project venv (make setup). scan_contract fetches verified ABIs from Sourcify and needs network access."
}
